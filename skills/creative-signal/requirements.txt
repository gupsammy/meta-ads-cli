# creative-signal core — the ONLY required pip dependency (spec §11).
# Deterministic features run through ffmpeg/ffprobe + stdlib; stats are stdlib.
# Floor pin only: reproducible-enough to debug, loose enough not to fight pip.
google-genai>=1.0
